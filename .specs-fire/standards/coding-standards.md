# Coding Standards

- Python: Use type hints, follow PEP8, `black` for formatting
- TypeScript: Strict mode, explicit prop types, `eslint` + `prettier`
- Tests: Unit tests for core logic; integration tests for API boundaries

Generated by FIRE orchestrator.
