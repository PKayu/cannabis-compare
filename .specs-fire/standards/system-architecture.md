# System Architecture

High-level overview:

- Frontend: Next.js app in `frontend/` serving the UI
- Backend: FastAPI app in `backend/` providing REST endpoints
- Database: PostgreSQL (Supabase) for persistent storage

This is a starter artifact created by FIRE; update with architecture diagrams as needed.
